name: Build NopCommerce Plugin

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore packages
        run: dotnet restore src/Nop.Plugin.Payments.SimplePay/Nop.Plugin.Payments.SimplePay.csproj

      - name: Build plugin
        run: dotnet build src/Nop.Plugin.Payments.SimplePay/Nop.Plugin.Payments.SimplePay.csproj -c Release

      - name: Package plugin
        run: |
          mkdir -p package/Presentation/Nop.Web/Plugins/Nop.Plugin.Payments.SimplePay
          cp -r src/Nop.Plugin.Payments.SimplePay/bin/Release/net8.0/* package/Presentation/Nop.Web/Plugins/Nop.Plugin.Payments.SimplePay/
          cd package
          zip -r ../Nop.Plugin.Payments.SimplePay.zip .

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          files: Nop.Plugin.Payments.SimplePay.zip
